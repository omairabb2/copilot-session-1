services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-jwt-backend
    ports:
      - "8000:8000"
    environment:
      JWT_SECRET_KEY: "super-secret-key-change-me"
